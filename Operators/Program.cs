using System;

namespace Operators
{
    class Program
    {
        static void Main(string[] args)
        {
            string name = string.Empty;
           
            if (name != string.Empty)
            {
                Console.WriteLine(name);
            }

            int number1 = 14;
            int number2 = 15;

            if (number1 != 0 || number2 != 0)
            {
                Console.WriteLine("Değer girilmiş");
            }

        }
    }
}
